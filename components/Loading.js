import React from 'react'

function Loading() {
    return (
        <div className=" h-screen w-full from-white to-zinc-200 bg-gradient-to-br text-center flex items-center justify-center">
            <p className=" text-3xl font-bold text-zinc-300 animate-pulse">Shopsa Loading . . .</p>
        </div>
    )
}

export default Loading